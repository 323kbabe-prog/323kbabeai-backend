# .env.example for Info Set v1.2 — KV Mirror (safe)

# Required: your OpenAI API key
OPENAI_API_KEY=sk-xxxx

# Optional: Spotify keys if you want audio features
#SPOTIFY_CLIENT_ID=your_spotify_client_id
#SPOTIFY_CLIENT_SECRET=your_spotify_client_secret

# Domains allowed to connect to backend
CORS_ALLOW=https://1ai323.ai,https://www.1ai323.ai

# Default art style
DEFAULT_ART=film-editorial

# Default fan style (if needed for portrait mode)
DEFAULT_STYLE=stan-photocard

# Port (Render will set automatically, but keep for local dev)
PORT=10000
