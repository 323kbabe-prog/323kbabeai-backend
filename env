# .env.example — copy to .env locally; on Render set in Dashboard

# Required
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional (only if it matches your key's org)
#OPENAI_ORG_ID=org_XXXXXXXXXXXX

# Optional defaults
DEFAULT_STYLE=stan-photocard

# Optional for live Spotify trends + audio-vibe mapping
#SPOTIFY_CLIENT_ID=your_spotify_client_id
#SPOTIFY_CLIENT_SECRET=your_spotify_client_secret

# Optional
#PORT=10000
